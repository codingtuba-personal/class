```
## ALL COMMANDS TO BE RUN IN TERMINAL

# to clone repo into desktop:
cd ~/desktop && git clone https://github.com/codingtuba/class.git && code ./class
# if it doesn't work, run
rm -r ~/desktop/code

# to pull latest changes:
cd ~/desktop/class && git pull -f && code .

```
